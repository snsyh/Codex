<p id="notice"><%= notice %></p>

<%= form_for @front_search_condition, as: :sc, url: '/demos', :method => :get do |f| %>
<li><%= f.text_field :name %></li>
<li><%= f.text_field :title %></li>
<li><%= f.text_field :price %></li>
<li><%= f.select(:prefecture_type_id, options_from_collection_for_select(@prefectures, :id, :name, @front_search_condition.prefecture_type_id.to_i), :include_blank => true) %></li>
<%= f.submit '検索' %>
<% end %>

<% session[:cart].each do |key,value| %>
<%= key %>:::::::::::::::::::::::::<%= value %>
<br/>
<% end %>

<% @items.each do |item| %>
[<%= item.item_code %>:::<%= item.item_name %>]
<%= link_to 'カート追加', controller: "demos", action: "addcart" , shop_code: item.shop_code, item_code: item.item_code %>
<%= link_to 'カート削除', controller: "demos", action: "removecart", shop_code: item.shop_code, item_code: item.item_code %>
<br/>
<% end %>
<%= link_to 'クリア', controller: "demos", action: "clearcart" %>
<br/>

<div id="cli">
  click&nbsp;click&nbsp;click
</div>
aaaaaaaaaaaa
<div id="testes">
  はっひふっへほー
</div>

<%= paginate @demos %>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Title</th>
      <th>Price</th>
      <th>Prefecture</th>
      <th>Region</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @demos.each do |demo| %>
    <tr>
      <td><%= demo.name %></td>
      <td><%= demo.title %></td>
      <td><%= demo.format_price %></td>
      <td><%= demo.prefecture_type.name %></td>
      <td><%= demo.region_name(demo.prefecture_type).name %></td>
      <td><%= demo.thumnails %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<br>

