<p id="notice"><%= notice %></p>

<%= form_for @front_search_condition, as: :sc, url: '/demos', :method => :get do |f| %>
<li><%= f.text_field :name %></li>
<li><%= f.text_field :title %></li>
<li><%= f.text_field :price %></li>
<li><%= f.select(:prefecture_type_id, options_from_collection_for_select(@prefectures, :id, :name, @front_search_condition.prefecture_type_id.to_i), :include_blank => true) %></li>
<%= f.submit '検索' %>
<% end %>

<div id="testes">
  はっひふっへほー
</div>

<% @items.each do |item| %>
[<%= item.item_name %>]

<input type="button" value="追加" class="addcart" id="<%= item.shop_code %>_<%= item.item_code %>">
<input type="button" value="削除" class="removecart" id="<%= item.shop_code %>_<%= item.item_code %>">
<br/>
<% end %>
<input type="button" value="クリア" class="clearcart">
<br/>

<div id="cli">
  click&nbsp;click&nbsp;click
</div>
aaaaaaaaaaaa

<%= paginate @demos %>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Title</th>
      <th>Price</th>
      <th>Prefecture</th>
      <th>Region</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @demos.each do |demo| %>
    <tr>
      <td><%= demo.name %></td>
      <td><%= demo.title %></td>
      <td><%= demo.format_price %></td>
      <td><%= demo.prefecture_type.name %></td>
      <td><%= demo.region_name(demo.prefecture_type).name %></td>
      <td><%= demo.thumnails %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<br>

